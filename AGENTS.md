# DAS Monorepo

Railway Driver Advisory System. See each module's `AGENTS.md` for specific guidelines:

`das_client/`, `das_backend/`, `das_admin_tool/`, `das_e2e_testsuite/`, `sfera_mock/`, `das_playground/`

Note: treat `docs/` as potentially outdated for now, unless the task explicitly targets docs.
