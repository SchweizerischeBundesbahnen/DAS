// allows facade of package_info_plus app version and build number
class AppInfo {
  final String version;
  final String buildNumber;

  const AppInfo({
    required this.version,
    required this.buildNumber,
  });
}
