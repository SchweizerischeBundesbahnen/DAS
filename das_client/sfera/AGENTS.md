# sfera – Agent Guide

SFERA XML protocol integration. Parses and validates SFERA XML (schema in `SFERA.xsd`) received over MQTT into a large
set of typed journey domain models, and maintains a local drift-backed cache so journey data survives offline.

