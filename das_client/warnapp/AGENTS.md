# warnapp – Agent Guide

Detects train start/stop from device motion (accelerometer via `sensors_plus`) and geolocation, so the app can warn the
driver appropriately. `algorithmus`/`rechner` are the original German domain terms (algorithm/calculator) — kept
intentionally, don't rename to English.
