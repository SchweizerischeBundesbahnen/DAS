name: auth
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.6.2

resolution: workspace

dependencies:
  # https://pub.dev/packages/sbb_oidc
  sbb_oidc: ^4.1.0

dev_dependencies:
  # https://pub.dev/packages/flutter_lints
  flutter_lints: ^5.0.0
