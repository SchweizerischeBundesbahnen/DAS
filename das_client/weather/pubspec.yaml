name: weather
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  http_x: 1.0.0
  logging: ^1.3.0
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

