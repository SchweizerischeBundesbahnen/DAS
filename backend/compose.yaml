services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=driver_advisory_system'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'
